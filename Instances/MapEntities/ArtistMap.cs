using System;

namespace ShareInstances.Instances.Map;
public record ArtistMap(byte[] Buid,
                        string Name,
                        string Description,
                        byte[] Avatar);